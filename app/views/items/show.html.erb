<p id="notice"><%= notice %></p>

<p>
  <strong></strong>
  <%= @item.description %>
</p>

<%= link_to 'Back to my list', items_path %>   |
<% if @item.user == current_user %>
	<%= link_to 'Edit', edit_item_path(@item) %> | 
	<%= link_to 'Delete' , items_path, method: :delete, data: { confirm: 'Are you sure?' } %> 
<% end %>

